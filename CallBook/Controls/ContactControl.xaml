<UserControl x:Class="CallBook.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallBook.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="300">
    <Grid Background="#ccc">
        <StackPanel>
            <TextBlock x:Name="nameTextBlock" FontSize="20"
                                   FontWeight="Bold"/>
            <TextBlock x:Name="emailTextBlock" Text="{Binding Email}" FontSize="15"
                                   Foreground="DodgerBlue"/>
            <TextBlock x:Name="phoneTextBlock" Text="{Binding Phone}" FontSize="15"
                                   FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>
